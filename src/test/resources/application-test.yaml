spring:
  datasource:
    url: jdbc:h2:mem:market
    driver-class-name: org.h2.Driver
    username: market
    password: market
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
